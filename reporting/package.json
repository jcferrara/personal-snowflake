{
  "name": "@personal-snowflake/reporting",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Observable Framework reporting layer over the personal-snowflake ANALYTICS database in Snowflake.",
  "scripts": {
    "dev": "observable preview",
    "build": "observable build",
    "deploy": "observable deploy",
    "clean": "observable clean"
  },
  "dependencies": {
    "@observablehq/framework": "^1.13.0",
    "dotenv": "^16.4.0",
    "snowflake-sdk": "^1.14.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "@types/snowflake-sdk": "^1.6.24",
    "typescript": "^5.7.0"
  },
  "engines": {
    "node": ">=18"
  }
}
